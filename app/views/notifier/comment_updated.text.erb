Hello!
<%= @comment.user %> has just updated the <%= @comment.ticket.title %> ticket for <%= @comment.project.name %>

They wrote:
<%= @comment.text %>

You can view this ticket on line by going to:
<%= project_ticket_url(@comment.project, @comment.ticket) %>
